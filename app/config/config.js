const DEVTOOLSPORT = 8000;

//You may need to modify the baseUrl if your context is different from the api endpiont.
var appConfig = {
  nativeReduxDevToolsPort: DEVTOOLSPORT,
  baseUrl: 'https://swap-node.herokuapp.com/untappd/',
};

module.exports = appConfig;
